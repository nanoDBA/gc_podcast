{
  "name": "gc-podcast-scraper",
  "version": "1.0.0",
  "description": "General Conference metadata and audio link extractor",
  "type": "module",
  "scripts": {
    "dev": "npx tsx src/index.ts",
    "feed": "npx tsx src/generate-feed.ts",
    "generate-index": "npx tsx src/generate-index.ts",
    "scrape-all": "npx tsx src/scrape-all.ts",
    "update": "npm run scrape-all && npm run feed",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src tests --ext .ts",
    "format": "prettier --write src tests",
    "format:check": "prettier --check src tests",
    "prepare": "husky"
  },
  "keywords": [
    "general-conference",
    "lds",
    "podcast",
    "scraper"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0 <24.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^9.0.0",
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0",
    "prettier": "^3.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.3.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "lint-staged": {
    "src/**/*.ts": [
      "eslint --fix"
    ],
    "tests/**/*.ts": [
      "eslint --fix"
    ]
  }
}
